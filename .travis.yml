sudo: required
services:
  - docker

before_install:
  - docker build -t saveup/saveup-frontend .
  - docker build -t saveup/saveup-frontend-tests

script:
  - docker run saveup/saveup-frontend-tests npm run test

deploy:
  provider: elasticbeanstalk
  region: "us-east-2"
  app: "SAVEUP-FRONTEND"
  env: "Saveupfrontend-env"
  bucket_name: "elasticbeanstalk-us-east-2-855433857104"
  bucket_path: "saveupfrontend"
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
